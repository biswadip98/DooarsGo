export default function AdminPlaceholder({ title }) {
  return (
    <div>
      <h2 className="font-display text-xl font-bold text-forest mb-1">{title}</h2>
      <div className="bg-white rounded-2xl border border-mist p-5 text-sm text-ink/60">
        This section is built in the next step (Phase 6b).
      </div>
    </div>
  )
}
